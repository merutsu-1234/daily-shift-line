name: Send Daily Shift

on:
  schedule:
    - cron: '0 22 * * *' # 毎朝7時 JST
  workflow_dispatch: # 手動実行用

jobs:
  send-shift:
    runs-on: ubuntu-latest
    steps:
      - name: Call Supabase Edge Function
        run: |
          curl -X POST https://<https://rqilzkvvybxvclgrefhw.supabase.co/functions/v1/line-send-daily-shift
>.functions.supabase.co/send-daily-shift
